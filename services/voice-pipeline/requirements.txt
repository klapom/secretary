# Voice Pipeline - XTTS + Whisper with GPU acceleration
# CUDA 13.0 compatible versions (Python 3.12)

# Core ML frameworks (already installed)
# torch>=2.5.0
# torchaudio>=2.5.0

# STT - Whisper with faster implementation
openai-whisper
faster-whisper>=1.1.0

# Audio processing
librosa>=0.10.0
soundfile>=0.12.0
pydub>=0.25.1

# API server
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.20

# Utilities (numpy already installed with torch)
scipy>=1.11.0

# For XTTS, we'll use xtts-api-server or install via git
# Will install separately after basic deps
