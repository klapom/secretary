# XTTS v2 (Coqui TTS) Dependencies

# Core TTS (Python <3.12 required)
TTS>=0.22.0

# Audio processing
soundfile>=0.12.0
librosa>=0.10.0
pydub>=0.25.1

# API server
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6

# Utilities
# Note: numpy version is managed by TTS (requires numpy==1.22.0 on Python 3.10)
scipy>=1.11.0
