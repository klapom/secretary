# EchoMimic V3 dependencies for DGX Spark (ARM64)
# Note: torch==2.10.0+cu130 is pre-installed in Dockerfile before these
#
# tensorflow-cpu: retina-face face-detector requires TF, but only for inference
# → CPU version is fine (face detection is not on the hot path) and has ARM64 wheels

Pillow>=10.0.0
einops>=0.7.0
safetensors>=0.4.0
timm>=0.9.0
tomesd>=0.1.3
torchdiffeq>=0.2.3
torchsde>=0.2.6
datasets>=2.14.0
numpy==1.26.4
scikit-image>=0.21.0
opencv-python==4.10.0.84
omegaconf>=2.3.0
sentencepiece>=0.1.99
albumentations>=1.3.0
imageio[ffmpeg]>=2.34.0
imageio[pyav]>=2.34.0
tensorboard>=2.14.0
beautifulsoup4>=4.12.0
ftfy>=6.1.0
func_timeout>=4.3.5
onnxruntime>=1.18.0
accelerate>=0.25.0
diffusers>=0.31.0
transformers>=4.46.2
moviepy==2.2.1
librosa>=0.10.0
pyloudnorm>=0.1.1

# Face detection — InsightFace instead of retina-face+tensorflow
# retina-face requires tensorflow-cpu which has no Python 3.10 ARM64 wheel
# InsightFace is already proven on our ARM64/GB10 setup (used in LivePortrait)
insightface>=0.7.3

# API server
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6

# HuggingFace model downloads
huggingface-hub>=0.20.0
modelscope>=1.9.0
