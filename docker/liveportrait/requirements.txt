# LivePortrait Dependencies for DGX Spark (ARM64)
# Note: torch==2.10.0+cu130 is pre-installed in Dockerfile before these

# Core LivePortrait deps (from upstream requirements_base.txt)
numpy==1.26.4
pyyaml==6.0.1
opencv-python==4.10.0.84
scipy==1.13.1
imageio==2.34.2
lmdb==1.4.1
tqdm==4.66.4
rich==13.7.1
onnx==1.16.1
scikit-image==0.24.0
albumentations==1.4.10
imageio-ffmpeg==0.5.1
tyro==0.8.5
pykalman==0.9.7
pillow>=10.2.0

# ONNX Runtime CPU-only (no ARM64 GPU wheels exist)
# Used only for InsightFace face detection/landmarks
onnxruntime==1.18.0

# InsightFace for face detection + cropping
insightface>=0.7.3

# HuggingFace for model download
huggingface-hub>=0.20.0

# transformers pinned <4.43.0 (same as XTTS — safe default)
transformers>=4.38.0,<4.43.0

# API server
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6
