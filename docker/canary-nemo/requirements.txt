# NVIDIA Canary with NeMo Dependencies

# Audio processing
librosa>=0.10.0
soundfile>=0.12.0
audioread>=3.0.0

# API server
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6

# Utilities
numpy>=1.24.0
scipy>=1.11.0

# Note: NeMo is installed separately in Dockerfile due to size
